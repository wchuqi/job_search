# 算法学习资料：栈、队列、单调结构和堆

[返回索引](../算法学习资料.md)

## 学习目标

- 掌握栈、队列和堆的基本用法。
- 理解单调栈、单调队列维护候选元素的机制。
- 能解决括号、下一个更大元素、Top K、滑动窗口最大值等问题。

## 理论导读

栈和队列控制处理顺序：栈是后进先出，适合处理最近未匹配元素；队列是先进先出，适合 BFS 和按时间顺序处理。堆维护动态集合中的最小或最大元素，适合 Top K 和优先级调度。

单调结构的本质是“淘汰不可能成为答案的候选”。当一个新元素进入时，如果旧元素在位置和数值上都不如新元素，就可以被永久弹出。

## Java 21 示例：Top K

```java
import java.util.*;

class Solution {
    public int findKthLargest(int[] nums, int k) {
        PriorityQueue<Integer> heap = new PriorityQueue<>();
        for (int x : nums) {
            heap.offer(x);
            if (heap.size() > k) {
                heap.poll();
            }
        }
        return heap.peek();
    }
}
```

## Python 3 示例：Top K

```python
import heapq

class Solution:
    def findKthLargest(self, nums: list[int], k: int) -> int:
        heap: list[int] = []
        for x in nums:
            heapq.heappush(heap, x)
            if len(heap) > k:
                heapq.heappop(heap)
        return heap[0]
```

## 单调栈场景

- 下一个更大元素。
- 每日温度。
- 柱状图最大矩形。
- 接雨水。

## 练习

- LeetCode 20：有效的括号。
- LeetCode 84：柱状图中最大的矩形。
- LeetCode 155：最小栈。
- LeetCode 215：数组中的第 K 个最大元素。
- LeetCode 239：滑动窗口最大值。
- LeetCode 739：每日温度。

## 验收

- 能解释栈内元素保持什么顺序。
- 能说明堆大小为什么保持为 `k`。

> **重点：** 单调栈弹出元素时，通常就是确定该元素答案的时刻。
>
> **难点：** 单调队列既要维护值的单调性，也要维护下标是否过期。
>
> **易错：** Java `PriorityQueue` 默认是小顶堆；Python `heapq` 也默认小顶堆。

